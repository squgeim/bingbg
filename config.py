#!/usr/bin/python

conf = {
	# The resolution to download (if the given resolution is not found, 
	# your current desktop resolution will be used):
	'res':'1920x1200',

	# The destination to download to (note the / at the end). Make sure you 
	# have write permissions to that directory:
	'des':'/media/0xsda5/Media/Wallpapers/Bing/'
}
